"""Test suite for the behavioral anomaly detection system.

Tests are fast, CPU-only and deterministic. Every build phase adds its own module here and
the whole suite must stay green before the next phase begins.
"""
