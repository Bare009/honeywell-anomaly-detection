# Read API container dependencies -- deliberately minimal.
# No ML libraries here: this service only queries MongoDB, so the image stays small and
# starts fast. Versions match the root requirements.txt.

fastapi==0.111.1
uvicorn[standard]==0.30.3
websockets==12.0

motor==3.5.1
pymongo==4.8.0  # pinned: motor 3.5.x is incompatible with pymongo>=4.9
redis==5.0.7

pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1
